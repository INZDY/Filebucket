# Rendered Markdown editing over source fidelity

Filebucket notes use one rendered Markdown editing surface rather than separate raw Markdown write and preview modes because the primary note workflow should feel like direct in-place editing. Markdown remains the stored and exported format, but the editor may normalize Markdown formatting when saving; Export writes the stored Markdown body as Filebucket saved it instead of promising original source-format preservation.
